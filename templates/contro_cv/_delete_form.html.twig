<form method="post" action="{{ path('contro_cv_delete', {'id': cvs.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cvs.id) }}">
    <button class="btn">Delete</button>
</form>
