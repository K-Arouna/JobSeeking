{% extends 'base.html.twig' %}

{% block title %}Cv{% endblock %}

{% block body %}
    <h1>Cv</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cvs.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ cvs.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ cvs.prenom }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ cvs.age }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ cvs.adresse }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ cvs.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ cvs.telephone }}</td>
            </tr>
            <tr>
                <th>NiveauEtude</th>
                <td>{{ cvs.niveauEtude }}</td>
            </tr>
            <tr>
                <th>Experience</th>
                <td>{{ cvs.experience }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contro_cv_index') }}">back to list</a>

    <a href="{{ path('contro_cv_index', {'id': cvs.id}) }}">edit</a>

    {{ include('contro_cv/_delete_form.html.twig') }}
{% endblock %}
